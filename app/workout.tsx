import React from "react";
import { SafeAreaView, Text, View } from "react-native";

const workout = () => {
  return (
    <SafeAreaView>
      <View>
        <Text>workout</Text>
      </View>
    </SafeAreaView>
  );
};

export default workout;
